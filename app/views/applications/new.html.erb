<%= render partial: 'form', locals: {
    path: "/applications",
    method: :post,
    button_text: "Submit",
    fields: [:name, :street_address, :city, :state, :zip_code]
  } 
%>

<% if params[:error] %>
  <%= "Please fill in all fields." %>
<% end %>