<%= render partial: 'applicants_info' %>

<p>Pet(s) Applying for: </p>
<% @application.pets.each do |pet|%>
  <p><%= link_to "#{pet.name}", "/pets/#{pet.id}" %></p>
<%end%>
<br>

<div id="description">

  <% if @application.pets.size >= 1 && @application.status == "In Progress" %>
    <%= form_with url: "/applications/#{@application.id}/update", method: :patch, local: true do |form| %>
    <%= form.label :description, "Please describe why you would make a good home for this pet" %>
    <br>
    <br>
    <%= form.text_field :description %>
    <%= form.submit "Submit application" %>
    <% end %>
  <% end %>
</div>

  <div id="search">
  <% if @application.status == "In Progress" %>

    <h3>Add a Pet to this Application</h3>

    <%= form_with url: "/applications/#{@application.id}", method: :get, local: true do |form| %>
      <%= form.label :search %>
      <%= form.text_field :search %>
      <%= form.submit "Search for Pet" %>
    <% end %>
  <% end %>
  </div>


  <div id="adoption">
    <% @pets.each do |pet|%>
    <div id= "pet_<%=pet.id%>">
      <p><%= link_to "#{pet.name}", "/applications/#{pet.id}" %>
      <%= button_to "Adopt this Pet", "/applications/#{@application.id}/#{pet.id}", local: true, method: :post %></div>
  <% end %>
  </div>
