<center><h1>Applicants Information</h1></center?
<p>Name: <%= @application.name %></p>
<p>Street Address: <%= @application.street_address %></p>
<p>City: <%= @application.city %></p>
<p>State: <%= @application.state %></p>
<p>Zip Code: <%= @application.zip_code %></p>
<p>Description: <%= @application.description %></p>

<p>Status: <%= @application.status %></p>

<h2> Who is this application for </h2>

<% @application.pets.each do |pet| %>
<%= link_to pet.name, "/pet/#{pet.id}" %>
<% end %>

<%= form_with url: "/applications/#{@application.id}", method: :get, local: true do |f| %>
<%= f.label :search %>
<%= f.text_field :search %>
<%= f.submit "Add a Pet to this application" %>
<% end %>
