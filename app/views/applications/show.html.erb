<h1><%= @application.name%>'s Application</h1>

<h4><p>Status:   <%= @application.status %></p>

<p>Address:   <%= @application.street_address %>, <%= @application.city %>, <%= @application.state %>, <%= @application.zipcode %></p></h4>

<p> Why I deserve a pet: <%= @application.description %> </p>

<p>Pet(s) Applying for: </p>
<% @application.pets.each do |pet|%>
  <p><%= link_to "#{pet.name}", "/pets/#{pet.id}" %></p>
<%end%>

  <h3>Add a Pet to this Application</h3>

<%= form_with url: "/applications/#{@application.id}", method: :get, local: true do |form| %>
  <%= form.label :search %>
  <%= form.text_field :search %>
  <%= form.submit "Submit" %>
<% end %>

<% if @pets != nil %>

  <% @pets.each do |pet|%>
    <p><%= pet.name %></p>
    <%=button_to "Adopt this Pet", "/pet_applications/#{@application.id}/#{pet.id}", local: true, method: :post %>

  <% end %>

<% end %>
