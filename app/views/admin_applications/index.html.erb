<h1>Admin Application Index Page</h1>

<% @applications.each do |application| %>
    <h3><%= application.name %></h3>
    <h3><%= link_to "Visit #{application.name}'s Application", "/admin/applications/#{application.id}" %></h3>
    <hr>
<% end %>