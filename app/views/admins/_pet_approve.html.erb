<%= form_with url: "/admin/applications/#{@application.id}", method: :get, local: true do |form| %>
  <%= form.hidden_field :approve, value: "Application Approved!" %>
  <%= form.submit "Approve This Application" %>
  <% end %>