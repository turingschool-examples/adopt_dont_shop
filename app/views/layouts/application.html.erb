<!DOCTYPE html>
<html>
  <head>
    <title>Adopt, Don't Shop</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
  </head>

  <body>
    <% flash.each do |type, msg| %>
      <div>
        <%= msg %>
      </div>
    <% end %>

    <header>
      <h1> <a href='/'><span id='title' style='margin-right:20px'>Pets R Us</span> </a>
        <nav id='navbox'>
          <a class='navigation divider' href='/pets'>Pets</a>
          <a class='navigation div_2'href='/shelters' >Shelters</a>
        </nav>
      </h1>
    </header>
    <article>
    <%= yield %>
    </article>
  </body>
</html>
