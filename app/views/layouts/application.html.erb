<!DOCTYPE html>
<html>
  <head>
    <title>Adopt, Don't Shop</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
  </head>

  <body style="background-image: url('https://dt2n0xjvnpvnu.cloudfront.net/articles/blog/images/img-requirements-to-adopt-a-pet.jpg'); background-size: contain; background-repeat: no-repeat; background-attachment: fixed;">
    <% flash.each do |type, msg| %>
      <div>
        <%= msg %>
      </div>
    <% end %>
    <section>
      <%= link_to "Home", "/" %> | <%= link_to "Start an Application", "/applications/new" %> | <%= link_to "My Applications", "/applications" %> | <%= link_to "Pets", "/pets" %> | <%= link_to "Shelters", "/shelters" %>
    </section>
    <section>
      <%= link_to "Admin", "/admins" %>
    </section>
    <%= yield %>
  </body>
</html>