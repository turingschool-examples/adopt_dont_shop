<h2>All Shelters</h2>


<% @shelters.each do |shelter| %>
  <div id="shelter-<%= shelter.id %>">
    <h4><%= shelter.name %></h4>
  </div>
<% end %>

<div id="pending-shelters">
<h2>Shelters with Pending Applications</h2>

  <% @shelters.each do |shelter| %>
    <% if shelter.has_pending_applications?%>
    <h4><%= shelter.name %></h4>
    <% end %>
  <% end %>
</div>
